POSTGRES_DB=securewatch_db
POSTGRES_USER=securewatch_user

# Generate locally. Never commit the real value.
POSTGRES_PASSWORD=

# Generate with:
# python -c "import secrets; print(secrets.token_urlsafe(64))"
JWT_SECRET_KEY=

ENVIRONMENT=production
DEBUG=false
